/* eslint-disable no-undef */
$(document).on('ready', () => {
  // INITIALIZATION OF HEADER
  // =======================================================
  const header = new HSHeader($('#header')).init();
});
